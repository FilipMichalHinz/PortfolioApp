
export interface Portfolio {
    id: number;                // Unique identifier
    portfolioName: string;         // First name of the user
    createdAt: Date;           // Creation date of the user
}
